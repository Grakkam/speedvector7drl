import 'package:malison/malison.dart';

class ColorScheme {
  static const projectionVec = Color.gray;
  static const projectionCursor = Color.white;
  static const crashed = Color.darkRed;
  static const openGround = Color.darkGray;
  static const roughGround = Color.brown;
  static const info = Color.blue;
  static const success = Color.green;
  static const danger = Color.red;
  static const warning = Color.yellow;
}
