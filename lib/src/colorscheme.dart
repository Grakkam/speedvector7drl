import 'package:malison/malison.dart';

class ColorScheme {
  static const player = Color.orange;
  static const vProjection = Color.darkOrange;
  static const vCursor = Color.lightOrange;
  static const vProjectionWarning = Color.red;
  static const opponent = Color.darkYellow;
  static const crashed = Color.red;
  static const openGround = Color.darkGray;
  static const roughGround = Color.brown;
}
